RewriteEngine On
RewriteBase /BatikWistara25/

# Biarkan file asli (CSS, JS, gambar) tetap bisa diakses
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SEO URL: /berita/slug → berita-slug.php?slug=slug
RewriteRule ^berita/([a-zA-Z0-9-]+)$ berita-slug.php?slug=$1 [L,QSA]
